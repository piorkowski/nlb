{% extends 'base.html.twig' %}

{% block title %}Reset hasła{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-5">
                <div class="card">
                    <div class="card-header">
                        <h3 class="mb-0">🔑 Reset hasła</h3>
                    </div>
                    <div class="card-body">
                        {% for flash_error in app.flashes('reset_password_error') %}
                            <div class="alert alert-danger">{{ flash_error }}</div>
                        {% endfor %}

                        <p>Podaj adres email użyty podczas rejestracji.</p>

                        {{ form_start(requestForm) }}
                        {{ form_row(requestForm.email) }}
                        <button class="btn btn-primary w-100">Wyślij link resetujący</button>
                        {{ form_end(requestForm) }}

                        <hr>
                        <a href="{{ path('app_login') }}">Powrót do logowania</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
